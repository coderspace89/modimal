import React from "react";
import PaymentFailed from "@/app/components/pages/checkout/failed/PaymentFailed";

const page = () => {
  return (
    <div>
      <PaymentFailed />
    </div>
  );
};

export default page;
